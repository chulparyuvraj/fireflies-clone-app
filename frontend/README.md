# Frontend — Fireflies Clone

Next.js (App Router) + TypeScript + Tailwind frontend. See the top-level `README.md` (one directory up) for full setup instructions, architecture, database schema, and API overview.

Quick start:

```bash
npm install
npm run dev
```

Requires the backend running at the URL set in `.env.local` (defaults to `http://localhost:8000`).
